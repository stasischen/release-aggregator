---
description: Phase 8 Build Staging (JSON Compilation). Generates game assets from verified CSVs.
---

# Phase 8: Build Staging

**Goal**: Compile `5_full_view` (CSV) into `8_output` (JSON). Since V5.1, this also extracts translations into `Strings_{lang}.json`.

**Context**: This is the "Compilation" step. It runs after Linguistic Content QA (Phase 5).

## Resources

- **SOP**: [[8_staging/8_staging_sop.md]]
