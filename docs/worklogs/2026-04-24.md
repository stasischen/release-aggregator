# Daily Worklog - 2026-04-24

## Session: 13:13 - 實作 P0 模組化學習架構 (SRS, Adaptive, Listening)

### Summary
- 實作了新版 Modular Study 的核心 P0 功能，包含解耦的 SRS 持久化層、基於歷史掌握度與當前表現的 Adaptive Learning 診斷與建議引擎，以及聽力練習渲染器雛形。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | c77ee705 | done |

### Pending Decisions
- none

### Blockers
- none

### Next Actions
1. 針對新架構的各類 node 進行更細緻的 `masteryKey` 定義，以提升 weakest-node 推薦的精準度。
2. 擴充 Listening Renderer 的實際音訊播放與答案檢核邏輯。

---

## Session: 21:20 - Modular Study: ReviewCardPayload & Listening Practice 落地

### Summary
- 完成 `ReviewCardPayload` 資料模型與映射層實作，達成 UI 與原始內容模型 (Sentence/Vocab) 的解耦。
- 強化 `UlvListeningRenderer` 穩定性，加入防禦性解析與錯誤回退 UI，避免惡意 Payload 造成 Lesson 崩潰。
- 修正 Retry 統計累加邏輯，改由 Viewer/Runtime 端統一處理增量統計，確保數據準確性。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | a7baa06d | done |

### Pending Decisions
- none

### Blockers
- none

### Next Actions
1. 整合 `audioplayers` 實現真實音訊播放。
2. 擴展 `ReviewCardPayload` 至 Typing 與 Scramble 等其他練習模式。

---

## Session: 22:23 - Modular Study: ReviewCardPayload Injection Point 實作

### Summary
- 在 `ModularLessonAdapter` 中實作了 `ReviewCardPayload` 的自動注入點 (Injection Point)，支援從 `Sentence` 與 `VocabItem` 原始資料自動生成 review payload。
- 建立核心欄位保護機制（IMMUTABLE core fields）與巢狀深層合併（Deep Merge）規則，確保數據一致性並防止內容覆寫導致解析失敗。
- 強化內容推導啟發式 (Heuristics)，明確區分顯式標記優先於啟發式推導的規則，並確保 `lessonId` 上下文完整傳遞。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | ba5de5c1 | done |

### Pending Decisions
- `metadata` 目前僅實作單層合併，若未來有更深層的巢狀需求需再擴充。

### Blockers
- none

### Next Actions
1. 針對 `word_review` 與 `cloze_review` 進行渲染器實作並接入此注入點。
2. 評估是否需要對 payload 中的 raw `sentence` / `vocab` 進行後處理清理，以維持 single source of truth。
