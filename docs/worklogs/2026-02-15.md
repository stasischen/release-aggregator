# Worklog - 2026-02-15

# Worklog - 2026-02-61. Run `flutter test`

62. Manual verification in App (A1 course).
2.

64. ### Phase A4: Legacy Content Integration

65.
5. - **Goal**: Enable viewing of legacy "Rich JSON" courses (Articles & Yarn Dialogues) in the Viewer.
6. - **Implementation**:
7. - Updated `legacy_builder.py` to scan `content/ko/article` and `content/ko/yarn`.
8. - Implemented "Rich JSON" parsing and tokenization mapping support.
9. - Injected legacy content into `viewer.html` under `Legacy_Article` and `Legacy_Dialogue` categories.
10. - Restricted locale support to `zh-TW` as per user request.

## Summary

Achieved 0% unresolved token ratio for Korean A1 content by refining the mapping strategy. Transitioned from hardcoded phrase mappings to a robust rule-based grammar system (hapnida, plurals, time/minutes). Updated the `gemini_startup_protocol` to standardize communication language (Traditional Chinese) and enforce worklog updates.

## Activities

- **Mapping Strategy Refinement**:
  - Implemented `verb_hapnida` rule to correctly decompose `감사합니다` -> `감사` + `하다` + `ㅂ니다`.
  - Added rules for `time_hour_numeric`, `time_minute_numeric`, `particle_deul`, `copula_ijyo`.
  - Removed obstructing hardcoded phrase mappings (`감사합니다`, `10분`).
  - Added comprehensive vocabulary roots to support rules.
  - **Result**: Unresolved Ratio dropped from 13.5% -> **0%** (Target Met).

- **Infrastructure**:
  - Migrated `.agent/workflows` to `release-aggregator` for cross-repo visibility.
  - Updated `gemini_startup_protocol.md` with:
    - Language Rule: Traditional Chinese.
    - Worklog Rule: Update on every commit.

- **Quality Assurance**:
  - Validated decomposition logic with `test_decomposition.py`.
  - Passed all Hard Gates (Schema) and Soft Gates (Coverage).

## Session 2: Mapping Architecture Stabilization & 100% Roadmap

- **Architecture Refactoring**:
  - Split monolithic `rules.json` (1138 lines) into 8 category-based files in `scripts/core/rules/`.
  - Implemented hash-based cache invalidation in `CacheManager` to ensure rule changes take immediate effect.
  - Updated `RulesEngine` to support directory-based rule loading with priority merging.
- **Rule Expansion & Testing**:
  - Improved coverage for irregulars (`추워요`, `고마워요`), observations (`사진이네요`), and complex particles (`이기도`).
  - Created a regression test suite (`tests/rule_tests.json`) with 14 foundational test cases (100% pass).
  - **Result**: Stable 8.6% unresolved ratio on a larger token set (5739 tokens).
- **Roadmap for 100% Resolution**:
  - Created `KO_RESOLUTION_100PCT_TASKS.json` in `release-aggregator` with 7 specific tasks.
  - Identified that ~40% of unresolved tokens are Yarn metadata that needs filtering.

---

## Session: 21:35 - KO-RES-08 Startup & Gap Analysis

### Summary

- Initialized Task [KO-RES-08] for frontend dictionary hydration.
- Approved implementation plan for bridging the gap between pipeline atoms and frontend dictionary.
- Marking task in-progress and initiating gap analysis.

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| release-aggregator | main | - | in_progress |

### Next Actions

1. Complete gap analysis of mapping atoms.
2. Update `dictionary_core.json` and language packs.
