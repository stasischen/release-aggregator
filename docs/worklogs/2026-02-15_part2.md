
# Worklog 2026-02-15 (Part 2)

## Updates
- **Mapping Strategy Refined**: Pivoted from "hard-coding phrases" to "proper grammar rules" as per user feedback (`不要為了達標去搞這種東西`).
- **New Rules Implemented**:
  - `verb_dwaeseo_contraction`: Handles `돼서` -> `되다` + `어서`.
  - `particle_ui_possessive`: Handles `의`.
  - `particle_deul_do`: Handles `들도`.
  - `verb_past_conditional`: Handles `었으면`.
- **Infrastructure**:
  - Updated `KoreanTokenizer` to support multiple dictionary files (`mapping.json`, `mapping_names.json`).
  - Added `mapping_names.json` for proper names to separate them from standard vocabulary.
  - Cleaned up temporary scripts in `scripts/ops`.

## Blockers / Remaining Unresolved Patterns
- **Current Unresolved Ratio**: ~8.9% (vs 0% artificial target).
- **Key Patterns to Address (Next Wave)**:
  - `밥의`: Still failing despite `particle_ui_possessive`. Priority check needed.
  - `의자들도`: Failing.
  - `6시예요`: Needs `time_hour_numeric_copula` rule.
  - `온라인이기도`: Needs complex particle `(이)기도`.

## Next Steps
- Continue adding grammar rules for the remaining top unresolved patterns.
- Investigate why `particle_ui_possessive` didn't catch `밥의` (possible priority or interaction issue).
