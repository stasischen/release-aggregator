# Daily Worklog - 2026-02-23

## Session: 00:00 - B1 Korean Audit Batch 3 (B1-11 to B1-15) Completion

### Summary

- **V5 Surgery Results**: Completed full manual audit and correction for 5 lessons (**B1-11 through B1-15**).
- **Heuristic Enhancements**: Significantly updated `fix_surgery.py` with rules for B1 mid-level patterns:
  - `면서` (while/as connectivity).
  - `니까/으니까` (causal connectives).
  - `라면/다면/ㄴ다면` (hypotheticals).
  - `텐데` standardized as `ko:n:터+ko:cop:이다+ko:e:ㄴ데`.
  - Plural dative particles `들에게` and `한테`.
- **Hallucination Cleanup**: Fixed recurring Gemini stem hallucinations (e.g., `없으다`, `좋으다`, `있으신가요`).
- **Gold Standard Sync**: Successfully synced all 5 lessons to `content/gold_standards/dialogue/B1/`.
- **Validation**: Performed full pipeline mapping validation of A1, A2, and B1. Unresolved ratio remains stable at 13.7%.

### Changed Repos

| Repo | Branch | Commit | Status |
| --- | --- | --- | --- |
| content-ko | main | committed | done |
| release-aggregator | main | committed | done |

### Next Actions

1. Proceed to **B1 Audit Batch 4** (B1-16 to B1-20).
2. Continue expanding `fix_surgery.py` with level-specific heuristics.

---

## Session: 00:06 - A2 Gold Standard Direct Fix & Audit Documentation

### Summary

- **A2 Gold Standard Batch Fix**: Developed and executed `scripts/ops/a2_v5_surgery_batch_01.py` to perform systematic V5 surgery across all 25 A2 lessons (A2-01 to A2-25).
- **POS Normalization**: Fixed recurring errors including `있다/없다/아니다` (V→ADJ), `오늘/어제/내일` (ADV→N), `왜` (PRON→ADV), `으시`→`시` honorific suffix.
- **Unicode Standardization**: Normalized all `ᄅ (U+1105)` to `ㄹ (U+3139)` in `gold_final_atom_id` fields.
- **Tense Correction**: Removed erroneous past-tense marker `+ko:e:었` in present-tense forms like `어때요` and `해요`.
- **Decomposition Fixes**: Decomposed `N+이+요` and `N+마다` compound forms.
- **Results**: Pipeline errors reduced from 1215 → 1098. Remaining errors are all dictionary gaps (missing atoms), not labeling errors.
- **Gap Report**: Created `docs/ops/A2_GOLDEN_FIX_LOG_2026-02-23.md` listing 20+ missing dictionary atoms (e.g., `ko:n:엄마`, `ko:v:조심하다`, `ko:prop:준호`) for future resolution.

### Changed Repos

| Repo | Branch | Commit | Status |
| --- | --- | --- | --- |
| content-ko | main | 110717f | done |
| release-aggregator | main | 549b6c4 | done |

### Pending Decisions

- `ko:e:ㄹ게요` 保持單一原子 vs. 拆分 `ko:e:ㄹ+ko:e:게요`：目前依據 A1 字典現狀維持單一原子。建議後續在 Rule Engine 優化時統一。

### Blockers

- 剩餘 1098 個 Pipeline 錯誤均為字典實體缺失（非格式錯誤）。需後續補齊缺失的動詞/名詞/專有名詞原子。

### Next Actions

1. 補齊 A2 字典缺失原子（`엄마`, `학기`, `교수님`, `태국`, `준호` 等，見 `A2_GOLDEN_FIX_LOG_2026-02-23.md`）。
2. 優化 Rule Engine：加入 `으시`→`시` 自動處理規則，避免人工逐條修復。
3. 完成 `KO-MAP-05-A2` 任務的 golden baseline 驗證。

---

## Session: 09:14 - B1 Korean Audit Batch 4 & 5 (B1-16 to B1-25) Completion

### Summary

- **V5 Surgery Results**: Completed full manual audit and correction for 10 lessons (**B1-16 through B1-25**).
- **Heuristic Enhancements**: Significantly expanded `fix_surgery.py` to handle B1 patterns:
  - Automated splitting of fused particles (`은/는/이/가/을/를/랑/이랑`).
  - Standardized `ㄹ게/을게` future intention endings.
  - Fixed auxiliary verb structures like `뒀어/보내 줘/해 봐`.
  - Added 20+ noun exclusions for the `는` suffix heuristic to prevent false positives.
- **Gold Standard Sync**: Finalized and synced 10 lessons to `content/gold_standards/dialogue/B1/`.
- **Validation**: Verified with `run_mapping_pipeline.py`. No new atom collisions or missing atoms found for the processed batch.

### Changed Repos

| Repo | Branch | Commit | Status |
| --- | --- | --- | --- |
| content-ko | main | 72e04fc | done |
| release-aggregator | main | ongoing | done |

### Next Actions

1. Proceed to **B1 Audit Batch 6** (B1-26 to B1-30).
2. Monitor pipeline stability for larger batch volumes.
