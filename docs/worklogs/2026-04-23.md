# Daily Worklog - 2026-04-23

## Session: 00:17 - Fix Flutter Lints to Unblock Git Push

### Summary
- 修復了 `lingo-frontend-web` 中 `ulv_flashcard_review_renderer.dart` 的 Flutter lint 錯誤（`unnecessary_import`, `sized_box_for_whitespace`）。
- 成功解除了 git push 的 pre-push hook 阻塞，並完成 commit 與 push。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | 19225c35 | done |
| release-aggregator | main | pending | done |

### Pending Decisions
- none

### Blockers
- none

### Next Actions
1. 繼續 `MDICT_KO_ZH_DICTIONARY_INGEST` 的相關工作。

---
## Session: 00:30 - P1 Practice Integration for Modular Runtime

### Summary
- 將 P1 Practice (Practice Card / Chunk Assembly) 正式接入 Modular Lesson Runtime。
- 實作了 `UlvPracticeRenderer` 並取代 placeholder，實現與 `ModularSession` 的深度狀態整合。
- 修復了錯題會誤進下一步以及組句區字卡無法取消選擇的關鍵互動 Bug。
- 新增並通過了完整的 Widget Test 覆蓋各種作答情境。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | 0e48ba8e | done |
| release-aggregator | main | 57af4225 | done |

### Pending Decisions
- none

### Blockers
- none

### Next Actions

---
## Session: 01:36 - Shared Sentence Atom Viewer Implementation

### Summary
- 完成了 `Dialogue` 與 `Article` 內容節點的 Shared Sentence Atom Viewer 實作。
- 實現了從 payload 到互動式句子行的資料映射，並新增了對發言者 (Speaker) 的顯示支援。
- 優化了 UI 美感與互動體驗（動畫、Chip 樣式、Dimming 非互動字卡），並強化了 Namespaced Support ID 的解析邏輯。
- 通過了 Widget Test 與 Integration Test，驗證了從點擊字卡到打開輔助面板的完整流程。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | 5396dc54 | done |
| release-aggregator | main | pending | done |

### Pending Decisions
- none

### Blockers
- none

### Next Actions
1. 檢視是否有其他 sentence-based 節點可以共用此 viewer。

---
## Session: 01:55 - Modular Lesson Composition & Node Sequence Shell

### Summary
- 建立了 `ModularLessonComposition` 輔助層，實現了基於 `contentForm` 與 `outputMode` 的語義化階段分類（Input, Practice, Review）。
- 統一了 Runtime 的推進合約與重置行為，確保節點切換時自動清空 Drawer 與 Anchor 狀態，提升流程穩定性。
- 新增了 `UlvCompositionIndicator` 指示器，並強化了 `ModularLessonRuntimeScreen` 的邊界檢查以防止空清單導致的 `RangeError`。
- 修復了因 `advance` 簽章變更導致的既有測試編譯錯誤，並新增了 Composition 與 Runtime 整合測試。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | 81afa634 | done |
| release-aggregator | main | pending | done |

### Pending Decisions
- none

### Blockers
- none

### Next Actions
1. 評估接入 Lesson Summary 頁面，顯示課程完成後的結果與分析。
2. 評估是否將 `LessonStepType` 從 presentation 層提升至 domain 層。

---
## Session: 11:23 - Modular Lesson Completion & Replay Implementation

### Summary
- 實作了 Modular Lesson Runtime 的結尾摘要頁面 (`UlvLessonSummaryView`)，取代原本的直接退出。
- 補齊了單課 `replay()` 契約，包含清除 `StudyProgress` 中的全域完成標記、重置 session 狀態並覆蓋持久化的節點進度。
- 建立了完整的測試防護網，涵蓋 Widget UI、Provider 邏輯以及 Runtime 層級的轉場分支。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | e1a26089 | done |
| release-aggregator | main | e7b9f55c | done |

### Pending Decisions
- none

### Blockers
- none

### Next Actions
1. 接入學習成效細節數據（如準確度、錯誤率）到摘要頁面。
2. 優化摘要頁面的視覺動效與互動細節。

---
## Session: 13:45 - Lesson Summary Result Breakdown & i18n Alignment

### Summary
- 升級了 `UlvLessonSummaryView`，從單純的完成頁轉型為詳細的結果報表頁面，新增了各階段（Input, Practice, Review）的完成度統計與逐節點的結果清單。
- 統一了節點標題的 i18n 查找邏輯，實作了 `ModularLessonComposition.getDisplayTitle` 輔助函式，遵循 `zh_tw -> en -> first value` 的 fallback 鏈，解決摘要頁標題失效的問題。
- 重構了 `ModularNodeViewer` 以共用上述 i18n 邏輯，減少冗餘程式碼並確保全平台一致性。
- 新增並通過了 Widget Test，驗證了 RichText 統計數據與 Localized 標題的正確渲染。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | 398f5dce | done |
| release-aggregator | main | pending | done |

### Pending Decisions
- none

### Blockers
- none

### Next Actions
1. 將 practice renderers（Typing, Scramble 等）的作答努力（retryCount, accuracy）串接到 `ModularSessionState` 以實現真實的成效報表。

---
## Session: 15:04 - Korean Gloss Backfill & v1.3.3 Reconciliation

### Summary
- 完成了 Batches 1-5 (250 筆) 的正式回補與對帳，成功將 **244 筆物理行** 寫入核心詞庫 `inventory/dictionary/2026-04-21/`。
- 建立了 **v1.3.3 驗證合約** 與自動化對帳表，解決了 245 READY / 243 Semantic / 244 Physical 的統計差異（包含 `분`, `끼치다` 的碰撞與 `대기` 的 1 對多匹配）。
- 穩固了 **LOG:LIMITER** 與 **COUNT:UNIT_VEHICLE** 等精確分類規格，並嚴格隔離 `금:1`, `길다`, `담:1` 等高風險隔離項。
- 驗證了回補腳本的 **Idempotency (等冪性)** 與 0-error 的驗證基線。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | 271d8550c | done |
| release-aggregator | main | pending | done |

### Pending Decisions
- `STAGING_ONLY` 的兩筆 (`법`, `날`) 需等其所屬 staging layer 正式併入 dated inventory 後再行回補。

### Blockers
- none

### Next Actions
1. 啟動 Batch 6 (251-300)，沿用 v1.3.3 合約進行 gloss 填充。
2. 定期重新執行 4-tier 驗證腳本以監控核心詞庫的一致性。

---
## Session: 15:27 - Practice Effort Metrics & Tracking Implementation

### Summary
- 實作了 Practice Effort Metrics (retryCount, success) 的追蹤與回報合約，建立 `PracticeEffortMetrics` 模型。
- 將 `typing_practice`, `sentence_scramble`, `speaking_challenge`, `practice_card` (P1) 與 `review_card` (P2) 等練習渲染器正式接入 Session 努力值收集。
- 更新了 `ModularLessonResult.aggregate` 與 `UlvLessonSummaryView`，使摘要頁面能顯示真實的作答成效（如重試次數、是否過關）而非落後推估值。
- 新增了 `modular_effort_metrics_test.dart` 測試，驗證努力值在節點間的隔離性、重播時的重置邏輯以及結果聚合的正確性。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | 17559e80 | done |
| release-aggregator | main | pending | done |

### Pending Decisions
- none

### Blockers
- none

### Next Actions
1. 接入 Time Tracking 語意，記錄各節點的停留時間。
2. 評估是否需要將努力值數據持久化到後端或 `ModularNodeProgress` 中。

---
## Session: 17:35 - Dictionary Emergency Restoration & v1.3.6 Upgrade

### Summary
- 緊急修復 v1.3.5 回補造成的 `ko:pron:것` 語義污染，恢復其 "東西 / 事情" 的正確定義。
- 將回補腳本升級至 v1.3.6，增加 Lemma 與 POS 強制檢查邏輯，並調整匹配優先級以防止 Rank Collision。
- 完成 Batches 1-5 (245 筆) 的安全重新回補與對帳，確認核心詞庫完整性後解除 Batch 6 的阻斷。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | 5c254313b | done |
| release-aggregator | main | e0f9643 | done |

### Pending Decisions
- none

### Blockers
- none

### Next Actions

---
## Session: 17:45 - Batch 6 (Rank 251-300) Dictionary Processing

### Summary
- 處理了 Batch 6 (Rank 251-300) 的 50 筆詞典條目。
- 遵循 **v1.3.6 Hardened Contract**：
    - 精煉繁體中文定義，並進行 `sense_id` 語義切分。
    - 嚴格遵循 `ko:[pos]:[lemma]` ID 格式與 `status: active`, `mapping_status: pending_review` 規範。
    - 標註 `metadata.origin`（漢字/外來語/固有詞）。
- 產出 `content_v2/staging/dictionary/batch_251_300.jsonl` 並通過 `validate_batch.py` 驗證。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | 241a5abf6 | done |
| release-aggregator | main | pending | done |

### Pending Decisions
- none

### Blockers
- none

### Next Actions
1. 啟動 Batch 7 (301-350)。

