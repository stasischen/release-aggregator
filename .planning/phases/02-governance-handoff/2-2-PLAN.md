# Plan 2-2: Closeout and Worklog Enforcement Alignment

## Goal
Make GOV-03 auditable by aligning closeout protocol, governance policy, and required reporting fields.

## Steps
1. Normalize required closeout fields across runbooks so `Blockers` and worklog path evidence are always mandatory.
2. Add a concise closeout checklist for `release-aggregator` sessions: worklog append, touched-repo state sync, and task-index sync.
3. Ensure references point to `docs/worklogs/YYYY-MM-DD.md` as the sole daily log target.

## Done When
- Closeout/governance docs share the same minimum required fields.
- A checklist exists that can be executed at session end without ambiguity.
- Worklog target is explicit and consistent in all touched active docs.
