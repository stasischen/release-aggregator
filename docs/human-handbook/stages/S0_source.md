# S0: Writer Source
# S0：內容來源

## Goal / 目標
Provide upstream lesson source materials.
提供上游課程來源資料。

## Input / 輸入
- Upstream writing assets (outside this repo)
- 上游寫作資產（此 repo 外）

## Output / 輸出
- Handoff inputs consumed by `content-ko` ingestion
- 提供給 `content-ko` ingestion 的交接輸入

## Notes / 備註
- `lllo` is upstream source, not release system-of-record.
- `lllo` 是上游資料來源，不是發版系統真相來源。
