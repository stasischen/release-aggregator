# Daily Worklog - 2026-04-27

## Session: 09:13 - Knowledge Lab UX Refactor (Batch D)

### Summary
- 完成 Knowledge Lab 前端體驗重構，將 `StudyGrammarHubScreen` 由「以課文為中心」轉為「以知識為中心」的兩欄式 (Master-Detail) 佈局。
- 補齊 `KnowledgeItem` 與 `Sentence` 模型中對齊 V5 Schema 的欄位 (`explanation_md`, `usage_notes_md`, `translation`)。
- 實作 `KnowledgePreviewPane` 並整合 `UlvSentenceAtomViewer` 以提供高保真例句渲染。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | codex/content-v2-frontendsync-checkpoint | 441f10b2 | done |
| release-aggregator | main | (pending) | done |

### Pending Decisions
- none

### Blockers
- none

---

## Session: 11:00 - Batch E Runtime Realignment & Main Branch Consolidation

### Summary
- 完成 Batch E: Runtime / Viewer Realignment。
- 引入 `StudyContentLocator` 集中管理資產路徑，消除 `VideoRepository` 與 `ModularLessonRepository` 中的路徑硬編碼。
- 整合 `LessonRegistryRepository` 以取代原始資產掃描，實現 Manifest-driven 的內容加載。
- 將 `release-aggregator` 與 `lingo-frontend-web` 的 `codex/content-v2-frontendsync-checkpoint` 分支併回 `main` 並推送。
- 解決了 `MACHINE_STATUS.md` 與歷史 `worklog` 的多處 Merge Conflicts。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | e97d9abd | done |
| release-aggregator | main | 13fcd25 | done |

### Pending Decisions
- none

### Blockers
- none

### Next Actions

---

## Session: (Earlier) - Video Atomization: Q7_UmOUi1XE (Phase C)
- **Video ID**: `Q7_UmOUi1XE`
- **Turns**: 57
- **Format**: `content_item.v1` (one object per word)
- **Status**: Promoted to `content_v2/inventory/content_assets/video/Q7_UmOUi1XE.json`

### Accomplishments
- Completed morphological atomization for the cooking vlog video.
- Standardized grammar splitting according to Tier 2 rules:
    - `ㄹ까요` -> `ㄹ + 까 + 요`
    - `볼게요` -> `보다(vx) + ㄹ + 게 + 요`
    - `안녕하세요` -> `ko:n:안녕하세요` (aligned with approved ledger entry)
    - `이다` chains fixed to use `이다 + 에요` (canonical form).
- Triaged 5 new lexical orphans into `manual_review_ledger.jsonl`:
    - `사골` (n)
    - `해동` (n)
    - `팔팔` (adv)
    - `숟갈` (count)
    - `초간단` (n)
- Verified coverage: 100% (Pass).

### Metadata
- Reprocess Queue updated.
- Ledger maintained (UTF-8 encoding without BOM).

---

## Session: 11:20 - Progress Review & Task Index Sync

### Summary
- 執行任務盤點與進度回顧，確認 Batch E (Runtime Realignment) 已完整併入主線。
- 更新 `TASK_INDEX.md`，將 `CONTENT_V2_FRONTEND_EXPORT_SYNC` 與 `SDA-007` 移至已完成區塊。
- 定義下一階段目標為 `UNIFIED_LESSON_VIEW_FLUTTER_TRANSFER`。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| release-aggregator | main | (pending) | done |

### Pending Decisions
- none

### Blockers
- none

### Next Actions
1. 啟動 `UNIFIED_LESSON_VIEW_FLUTTER_TRANSFER` 的 Implementation Plan 編寫。

---

## Session: 13:25 - ULV Flutter Transfer Review & Integration (ULVFT)

### Summary
- 審核並整合 ULV Flutter Transfer (ULVFT-001 to ULVFT-005) 的實作成果。
- 確認響應式 Shell 佈局與 Surface Adapters 的解耦設計完全符合 ULV 合約。
- 修復了 `UlvPrimarySurfaceAdapter` 的編譯路徑錯誤與 `Locale` 型別不匹配問題，並清理了前端 Lints 警告。
- 完成 `ModularLessonRuntimeShell` 的佈局測試與穩定性測試驗證。
- 更新任務總表進度為 6/8。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | 5f6b9d31 | done |
| release-aggregator | main | 093c9b4 | done |

### Pending Decisions
- none

### Blockers
- none

### Next Actions
1. 實作 ULVFT-006: 增加 QA 與回歸測試（含 loading / fallback / missing metadata 處理）。
2. 實作 ULVFT-007: 撰寫交接說明文件與剩餘缺口盤點。

---

## Session: 14:30 - ULV Flutter Transfer Completion (ULVFT-006 & ULVFT-007)

### Summary
- 完成 ULV Flutter Transfer 的最後兩項階段：QA 回歸測試 (ULVFT-006) 與交接結案 (ULVFT-007)。
- 增強 `UlvPrimarySurfaceAdapter` 與 `UlvSupportSurfaceAdapter` 的 Fail-Soft 健全度，補齊未實作 Surface 的佔位 UI。
- 建立 `ulv_qa_regression_test.dart` 覆蓋缺失 Meta、Legacy ID 標準化與 Switcher 路由等邊界情況。
- 撰寫 `ULVFT_HANDOFF_NOTES.md` 盤點當前 Shell 架構、已驗證表面與剩餘開發缺口。
- 更新 `TASK_INDEX.md` 將本任務移至「已完成待封存」區塊，進度 8/8。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | 5e66b647 | done |
| release-aggregator | main | (pending) | done |

### Pending Decisions
- none

### Blockers
- none

### Next Actions
1. 根據交接文件中的 Gaps (如 Article Renderer) 規劃下一階段內容。

---

## Session: 23:55 - Production Release Gating Pilot (PRG-005)

### Summary
- 完成 PRG-005: Pilot the release gate with dialogue and video first。
- 驗證 `assembler_prototype.py` 同步與過濾邏輯：實測 planning mode 抓出 candidate gaps，並成功將 staging-only candidate 提拔為 production release。
- 更新 `TASK_INDEX.md` 與 `PRODUCTION_RELEASE_GATING_TASKS.json` 狀態為 6/6 完成。
- 向使用者簡報「重新製作所有課程」時，PRG 將如何保護 production 邊界，並確立了下一步 `TARGET_LANG_COURSE_FACTORY` 重構的大方向。

### Changed Repos
| Repo | Branch | Commit | Status |
|---|---|---|---|
| release-aggregator | main | 7492209 | done |
| release-aggregator | main | 16c2353 | done |

### Pending Decisions
- 確認接下來「全部重做」要優先從哪一課或哪個 Unit 模板開始執行 (Layer 3 `TARGET_LANG_COURSE_FACTORY`)。

### Blockers
- none

### Next Actions
1. 準備啟動 `TARGET_LANG_COURSE_FACTORY` 或是相關的 UI 原型開發任務。
