# Worklog 2026-04-26

## 1. Dictionary Refactoring (Manifest-Driven)
- **Goal**: Move dictionary validation and export away from individual atom JSON files to a manifest/JSONL-based system.
- **Repository**: `content-ko`
- **Changes**:
    - Updated `export_to_app.py` to use `content_v2/inventory/dictionary/manifest.json`.
    - Updated `validate_dictionary_schema.py` to support manifest-based loading and mapping.
    - Updated `ko_data_pipeline.py` to use manifest for atom validation.
    - Added `--emit-atoms` opt-in flag (default false) to `generate_dictionary_core.py`, `generate_dictionary_i18n.py`, and `build_dictionary_inventory.py`.
    - Updated `stage3_executor.py` to use manifest for reporting.
- **Verification**:
    - Confirmed `export_to_app.py` works with an empty `atoms/` directory.
    - Confirmed `validate_dictionary_schema.py` correctly validates 7,017 atoms from JSONL shards.
    - Confirmed no atoms are generated by default in the standard pipeline.
- **Commit Hash**: `46e52169dca38216d84a4c0ec3ce520f4b79ebd7`

## 2. Frontend Dictionary Structure Upgrade (SDA-007)
- **Goal**: Upgrade `lingo-frontend-web` to support structured "Entry + Sense" definitions.
- **Repository**: `lingo-frontend-web`, `release-aggregator`
- **Changes**:
    - **Models**: Added `DictionarySense` and updated `DictionaryInventoryEntry` with `senses` list.
    - **Parser**: Refactored `DictionaryParser` to use `definitions[locale]` and removed all legacy fallbacks (Hard Break).
    - **UI**: Updated `DictionaryMeaningSection` to render multiple senses, grouped by `entry_no` with sub-numbering.
    - **Tasks**: Marked SDA-007 as `done` in `SCHEMA_DOCS_AUDIT_TASKS.json`.
- **Verification**:
    - Logic check for structured parsing from `definitions`.
    - UI check for grouping and numbering logic.
- **Commit Hash**: `b0f5d2ea` (frontend), `f230e17` (aggregator)

## 3. Unresolved Items (Next Steps)
- SDA-008: Perform full inventory validation against the schema.
- Fix `ProjectionSeedRepository` in `lingo-frontend-web`.
- Fix `SourceDetailScreen` in `lingo-frontend-web`.
- Add automated smoke test for dictionary export integrity without atoms.
