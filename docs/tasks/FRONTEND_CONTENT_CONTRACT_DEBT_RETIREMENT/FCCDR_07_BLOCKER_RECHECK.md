# fccdr-07 Blocker Recheck

Date: 2026-05-06
Task: `fccdr-07`
Status: still blocked on pipeline consumption

## Summary

`fccdr-07` cannot be completed yet.

The content-ko source-side blocker is partially resolved: canonical v2 Learning
Library i18n sidecars now exist under `content_v2/i18n/zh_tw/learning_library`.

The task remains blocked because `content-pipeline` still uses legacy i18n
bridge reads from `content/i18n/...`. `fccdr-07` is not retired until the
pipeline can emit runtime artifacts from canonical sidecars without legacy
reads.

## Current Evidence

- `content-ko/content_v2/i18n/zh_tw/learning_library` now contains canonical
  sidecars generated by `scripts/ops/build_learning_library_i18n_sidecars.py`.
- `scripts/ops/validate_learning_library_i18n_sidecars.py --locale zh_tw`
  passes with sentence coverage `3345/3346 = 99.97%` and knowledge coverage
  `218/218 = 100%`.
- Pipeline v2 output can produce runtime artifacts, but output i18n is still
  sourced from legacy bridge logic.
- Current frontend assets show Learning Library sentence i18n coverage of
  `3345/3346 = 99.97%`.
- The missing translated sentence is:
  `sent.src.ko.dialogue.a1_01.L01-D1-01`.

## Coverage By Surface

| Surface | Current Runtime Coverage | Source |
| --- | ---: | --- |
| shared/example bank sentences | 2133/2133 | canonical sidecar generated from v2 + migrated legacy i18n |
| video-derived source sentences | 1212/1212 | canonical sidecar generated from v2 + migrated legacy video i18n |
| Learning Library sentences total | 3345/3346 | canonical sidecar available; pipeline still uses legacy bridge |
| Knowledge i18n | 218 items | canonical sidecar generated from v2 + migrated legacy knowledge i18n |
| dialogue source sentence | 0/1 for `a1_01` missing row | missing canonical/legacy translation |

## Legacy Bridge Locations

Current `content-pipeline/pipelines/learning_library.py` still uses legacy
bridge paths at these decision points:

- knowledge i18n fallback comment around line 180
- `_load_legacy_learning_library_i18n("knowledge", item_id)` around line 183
- shared/example sentence legacy lookup around line 239
- source metadata legacy lookup around line 280
- video/dialogue/article sentence translation from legacy translations around
  line 309
- `_load_legacy_source_i18n` around line 365
- `_load_legacy_learning_library_i18n` around line 392

These bridge reads are acceptable only while `fccdr-07` remains blocked.

## Canonical Sidecars

Implementation contract:

- `content-ko/docs/tech/LEARNING_LIBRARY_I18N_SIDECAR_CONTRACT_V1.md`

Current canonical v2 sidecars:

- `content_v2/i18n/zh_tw/learning_library/manifest.json`
- `content_v2/i18n/zh_tw/learning_library/sources.json`
  - keyed by `source_id`
- `content_v2/i18n/zh_tw/learning_library/sentences.json`
  - keyed by `source_id` + `sentence_id`
  - video/dialogue/article rows preserve `turn_id`
- `content_v2/i18n/zh_tw/learning_library/knowledge.json`
  - keyed by `knowledge_id`

Shared/example bank, knowledge examples, video-derived source sentences,
dialogue sentences, and future article rows are covered through the single
`learning_library/sentences.json` `source_id -> sentence_id` model. The current
dialogue `A1-01` row is intentionally emitted with `status = needs_review`
because the legacy dialogue translation with the same local turn id does not
match the content_v2 Korean sentence.

## Retirement Conditions

`fccdr-07` can move from `blocked` to implementation-ready only when all of
these are true:

1. `content-ko` emits canonical v2 i18n sidecars under
   `content_v2/i18n/zh_tw/...` or another agreed manifest path. Done for
   Learning Library zh-TW sidecars.
2. `content-pipeline` v2 lookup order is:
   - canonical v2 sidecar first
   - legacy bridge only behind an explicit fallback flag
3. A fixture run disables or removes legacy `content/i18n` and still produces
   non-empty aligned i18n artifacts.
4. Frontend Learning Library i18n coverage gate passes from synced pipeline
   output.
5. `_load_legacy_source_i18n` and `_load_legacy_learning_library_i18n` can be
   retired, or quarantined behind a named fallback flag with owner and deadline.

## Next Tasks

1. Pipeline owner updates lookup order to canonical v2 sidecar first.
2. Pipeline owner adds a no-legacy fixture test.
3. Content owner fills the remaining `src.ko.dialogue.a1_01` `needs_review`
   translation before release.
4. Frontend sync validates that coverage remains at or above the current floor.
