# S2: Segmentation + Mapping (`content-ko`)

## Goal
Produce token/mapping aligned artifacts for downstream build.

## Main Work
1. Apply language segmentation/tokenization.
2. Generate and verify mapping artifacts.
3. Keep core/i18n split consistent with contracts.

## Exit Gate
- Required mapping/token artifacts exist and pass repo-level checks.
