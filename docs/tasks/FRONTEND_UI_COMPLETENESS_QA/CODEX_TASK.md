# Codex Task

## Scope

Repo: `/Users/ywchen/Dev/lingo/lingo-frontend-web`

Implement the low-risk product fixes first:

1. Learning Library mode should default to artifact mode or read an explicit config/env switch.
2. Video list card tap must navigate to the registered player route.
3. Video list should not expose a visible `coming soon` gallery action.

Then assess dictionary disambiguation and modular runtime placeholders as separate slices before
changing larger UI semantics.

## Validation

Run targeted Flutter tests for the touched feature areas, then `flutter analyze` if feasible.

