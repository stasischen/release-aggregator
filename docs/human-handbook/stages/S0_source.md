# S0: Writer Source

## Goal
Provide source lesson materials from upstream writing workflow.

## Input
- Upstream writing assets (outside this repo).

## Output
- Handoff inputs consumed by `content-ko` ingestion.

## Notes
- `lllo` is treated as upstream source, not release system of record.
