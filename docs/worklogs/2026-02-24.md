# Daily Worklog - 2026-02-24

## Session: 01:10 - C1 Dictionary Candidate Review & Evidence Staging

### Summary

- **C1-Only Candidate Generation**: Successfully generated a pure C1 dictionary candidate (`c1_candidate_20260223_pure_c1`) using `build_dictionary.py --gold-dir content/gold_standards/dialogue/C1`.
- **Mapping Verification**: Confirmed corrected mapping scope (3,797 entries) and isolated academic POS tagging. Verified zero "zombie tags" and documented 30 known coarse fallback residuals from the gold standards.
- **Evidence Signoff**: Created `REVIEW_APPROVED` within the staging directory as formal proof of review completion. 
- **Promotion Decision**: Deferred promotion to maintain production integrity (Partial Corpus Policy). Production remains consistent with the full A1-B2 corpus in `origin/main`.

### Changed Repos

| Repo | Branch | Commit | Status |
| --- | --- | --- | --- |
| content-ko | main | 3d87215e | done |
| release-aggregator | main | - | done |

### Pending Decisions

- Future full corpus promotion (A1-C1) should only be executed once B2 audit is fully finalized or when new systemic gold changes are required.

### Blockers

- none

### Next Actions

1. Re-initiate full corpus dictionary build (A1-C1) when B2 audit phases are complete.
2. Monitor if additional C1 academic vocabulary requires linter whitelisting.

---

## Session: 01:37 - Content Candidate Review Station MVP

### Summary

- **New Feature: content_candidate_review**: 在 `release-aggregator/tools/content_candidate_review` 初始化了一個全新的 Flutter Web 工具。
- **Phase 0 & 1 (Models & UI)**: 建立 `CandidateReviewItem` 模型、`HumanDecision` 枚舉與 Mock 資料。實作雙欄式審核介面（列表/詳情）。
- **Phase 2 & 3 (UX & Persistence)**: 新增進階篩選（狀態/類型）、搜尋（標題/摘要）以及即時進度追蹤。使用 `shared_preferences` 實作本地持久化。
- **Phase 4 & 5 (Operational Efficiency)**: 實作 JSON 匯入/匯出功能（支援剪貼簿貼上/複製）以及批次操作（批次淘汰/擱置）。
- **Quality Assurance**: 通過 `flutter analyze` 驗證，程式碼完全符合規範。

### Changed Repos

| Repo | Branch | Commit | Status |
| --- | --- | --- | --- |
| release-aggregator | main | - | done (new feature + docs) |

### Next Actions

1. 完善 Phase 6: 實作銜接 Catalog 與 Backlog Seed 的匯出適配器。
2. 增加「欄位缺失」警示機制（如缺少 can_do_zh_tw 時標記）。

---

## Session: 02:xx - Content Candidate Generation Framework Planning

### Summary

- **New Planning Task**: 在 `release-aggregator/docs/tasks/` 新增 `CONTENT_CANDIDATE_GENERATION_FRAMEWORK`，規劃候選內容生成的雙流程架構（API flow + Agent flow）。
- **Shared Contract First**: 定義了共用 schema / generation brief / QA report / review-ready bundle 的規劃方向，確保生成端與審核台可穩定銜接。
- **Downstream Adapters Included**: 將 `accepted_candidates.json -> catalog_draft.json / backlog_seed.json` 納入同一任務，以打通審核後落地流程。

### Changed Repos

| Repo | Branch | Commit | Status |
| --- | --- | --- | --- |
| release-aggregator | main | - | done (planning docs + task index update) |

### Next Actions

1. 先實作共用 schema 與 generation brief（AGG-GEN-001 ~ 003）。
2. 先跑 API flow MVP（AGG-GEN-004 ~ 008），產出可匯入審核台的 sample batch。

---

## Session: 16:20 - Audio Playback Stabilization (v4.5.5) - TTS Fallback & Regex Fix

### Summary

- **Zen UI Replay Fix**: 解決了在缺乏預錄音檔（如 B2/C1 課程）時，Dialogue Card 內部的 Replay 按鈕失效的問題。實作了自動語句切割 (Sentence Splitting) 並引入 TTS 降級機制。
- **macOS Robustness**: 優化了 `PrerecordedAudioService.seekToSentence(0)`，在 macOS 上改用完整重啟 (`playTurn`) 以避免 Platform Channel 在 `completed` 狀態下的 seek 競爭問題。
- **GoRouter Navigation Fix**: 修復了點選「文法索引」時產生重複 Page Key 的斷言錯誤（紅畫面）。透過將全域跳轉路由鎖定在 `rootNavigatorKey` 並移除衝突的動態 `ValueKey` 解決。
- **Unified Logic**: 統一了 Dialogue View 在 Prerecorded 與 TTS 兩種模式下的主動重播與單句點讀行為。

### Changed Repos

| Repo | Branch | Commit | Status |
| --- | --- | --- | --- |
| lingo-frontend-web | main | 40bdbc1 | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 持續監控 macOS 實機上 seek 於 completed 狀態後的穩定性。
2. 評估是否將 Article View 也比照辦理引入 Prerecorded Audio 支援（目前僅 TTS）。

