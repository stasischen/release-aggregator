# REQUIREMENTS

## v1 (Current)
- Maintain active task index and cross-repo task routing.
- Enforce startup/execution/closeout protocol consistency.
- Keep worklogs centralized in `docs/worklogs/YYYY-MM-DD.md`.

## v2 (Next)
- Add measurable quality gates for each repo handoff.
- Add recurring health checks for protocol drift.
- Add dashboard-style weekly summary from worklogs and task state.
