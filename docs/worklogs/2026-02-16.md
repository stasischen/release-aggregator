# 2026-02-16 Worklog

## [KO-RES-90] Resolve Low Confidence Tokens

### Context

Low confidence tokens in Korean pipeline prevented meeting accuracy targets (original ~1500). Confirmed issues with dictionary loading failure and unmapped particles.

### Implementation

- **dual-dictionary-loading**: Updated `KoreanTokenizer` and `RulesEngine` to support both mapping.json (strings) and dict.json (objects) simultaneously.
- **particle_ro_fix**: Corrected greedy regex and reconstruction template for `particle_ro` (failed `점심으로`).
- **manual_irregulars**: Added manual rules for `불렀어요` (call), `드네요` (cost/lift), `보이네요` (visible).
- **dictionary_additions**: Added missing nouns (`점심`, `경찰관`, `스트리밍`) to `dict_ko_zh_tw.json`.
- **pipeline_fix**: Updated `run_mapping_pipeline.py` to explicitly load dictionary.

### Results

- Phase 5 Audit (v11):
  - Low Conf: **0** (Target < 10)
  - Fallback: **0**
  - Polysemy: **0**

### Additional Commits

- `lingo-frontend-web`: Committed pending dictionary updates (from previous session).

### Verification

- Pipeline run and `audit_token_qa.py` confirmed 100% confidence pass.

---

## Session: 20:30 — Viewer Enhancement Task Creation

### Summary

- Created `VIEWER_ENHANCEMENT_TASKS.json` task epic (3 tasks: VIEWER-01 dictionary popup, VIEWER-02 old course loading, VIEWER-03 sync to lllo/tools).
- Added `format_evolution` section documenting anticipated frontend format changes (atoms in production dialogue, mapping.json as canonical source, senseIndex/contextMeaning testbed).
- Resolved git merge conflict in VIEWER_ENHANCEMENT_TASKS.json.

### Changed Repos

| Repo | Branch | Commit | Status |
| :--- | :--- | :--- | :--- |
| release-aggregator | main | `fcdd7ab` | done (VIEWER_ENHANCEMENT_TASKS.json) |

### Pending Decisions

- None for this session. Viewer tasks ready for agent assignment.

### Next Actions

1. Assign VIEWER-01 and VIEWER-02 to agents (can be parallel).
2. Begin KO-RES-08 (frontend dictionary hydration) after viewer work completes.

---

## Session: 12:30 — Korean Dictionary Audit & Tokenization Refinement

### Summary

- Audited `mapping_course.json`, removing redundant compound tokens and correcting miscategorizations.
- Refined `KoreanTokenizer` and `RulesEngine` to handle quotes (e.g., `'반'`) and missing particles (`쯤`, `죠`, `나`, `서`, honorific `si`).
- Updated `KoreanLemmatizer` to handle contractions like `재밌` -> `재미있다` and complex verb/adjective stems ending in `지`.
- Achieved a high-quality, normalized dictionary state with an extremely low unresolved ratio (~0.2%).

### Changed Repos

| Repo | Branch | Commit | Status |
| :--- | :--- | :--- | :--- |
| content-ko | main | `13a47aa` | done |
| release-aggregator | main | `d38f131` | done |

### Pending Decisions

- None.

### Next Actions

1. Fill `zh_tw` translations for the new dictionary entries (KO-RES-08).
2. Enhance rules engine for triple-layer compound resolution (KO-MAP-03).
