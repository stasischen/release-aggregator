# Archive

這裡存放已完成或已封存的 task 文件。

## Layout

- `YYYYMMDD/`：按完成或封存日期分組
- `YYYYMMDD/<file>`：該日對應的 plan、tasks、review、report 等歷史檔
- `YYYYMMDD/rules_engine_opt/`：少量需要保留原子目錄名稱的歷史工作區

## Rule

- 只查不改的文件放這裡
- 正在進行的任務留在 `docs/tasks/` 根層
- 可重用資產應另外進一步拆成專用子目錄
