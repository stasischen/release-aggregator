# Daily Worklog - 2026-02-21

## Session: 00:20 - Stable Branch Cleanup + Frontend Viewer Integration Plan

### Summary

- 從穩定版移除實驗性功能（Dream Weaver, Dev Launcher, 4 個實驗性遊戲路由 + UI 入口）
- 建立 macOS release build（135MB，分析體積構成）
- 完成 Frontend Viewer Integration (FVI) 規劃文件：將 Aggregator Viewer 已驗證的 3 大功能（預錄音檔+句子高亮+Grammar Notes）整合進 Flutter 前端
- 確定音檔壓縮策略：Opus (.ogg) 24kbps（實測 A1-01 從 1.0MB → 396KB，-61%）
- 4 項技術決策已定案：Convention-based audio path、獨立 GrammarNoteService、Stream-based highlight、Opus 格式

### Changed Repos

| Repo | Branch | Commit | Status |
| :--- | :--- | :--- | :--- |
| lingo-frontend-web | main | 407a640 | done |
| release-aggregator | main | e79c5db | done |

### Pending Decisions

- none (all 3 open decisions resolved: full A1 bundle, BottomSheet for grammar, flutter_markdown approved)

### Blockers

- none

### Next Actions

1. 執行 FVI Phase 1 (FVI-01~05)：AudioTimelineModel → PrerecordedAudioService → 句子高亮
2. 執行 FVI Phase 2 (FVI-06~09)：GrammarNote model → Markdown 面板 → 資產打包
3. 批次轉換 A1 音檔 MP3 → Opus (FVI-05a)

---

## Session: 01:25 - Frontend Viewer Integration Phase 1 Implementation

### Summary

- Completed FVI-01 to FVI-05: Implemented Pre-recorded audio playback and sentence-level highlighting into `lingo-frontend-web` stable branch.
- Batch converted A1 audio files to Opus 24kbps format, preserving payload size.
- Parsed `.sentences.json` successfully using newly built `AudioTimeline`.

### Changed Repos

| Repo | Branch | Commit | Status |
| :--- | :--- | :--- | :--- |
| content-ko | main | 3476c13 | done |
| lingo-frontend-web | main | 011d071 | done |
| release-aggregator | main | adfb192 | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 執行 FVI Phase 2 (FVI-06~09): GrammarNote model, Markdown 面板及資產打包

---

## Session: 01:55 - A1 Dictionary Fill + POS Standardization

### Summary

- 完成 A1 韓文詞典 800+ 項繁中翻譯補完，達成 A1 階層 100% 翻譯覆蓋率。
- 執行 A1 課程數據（Golden Samples）詞性標準化（修正 12 個檔案，如 `감사하다` 統一為 `adj`）。
- 撰寫 `docs/SOPs/KO_DICTIONARY_FILLING_PROTOCOL.md` 並落實「以課程需求為準，修正錯誤詞性標記」之原則。
- 重新產出 V5 字典與 `mapping.json`，確保數據流閉環。

### Changed Repos

| Repo | Branch | Commit | Status |
| :--- | :--- | :--- | :--- |
| content-ko | main | 141c586 | done |
| release-aggregator | main | TBD | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 啟動 A2/B1 階段之自動化中譯補充流程。
2. 進行 Token QA 對於多義詞 (Polysemy) 之深度審核。

---

## Session: 17:00 - Frontend Viewer Integration Phase 2 Implementation

### Summary

- Completed FVI-06 to FVI-09: Implemented `GrammarNote` model, `GrammarNoteService` and `GrammarNotesPanel` UI to display grammar lessons.
- Added 📝 icon button to `EventTopBar` opening a `DraggableScrollableSheet` with Markdown-rendered explanation content.
- Bundled A1-B1 Korean grammar JSON assets into the frontend.
- Fixed Riverpod/Freezed code-generation lints in `lingo-frontend-web`.

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | f0a8a0a | done |
| release-aggregator | main | b0665b2 | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 執行 FVI Phase 3: 資產反向連結與動態下載機制 (Asset downloading on-demand)

---

## Session: 02:15 - Resolve POS Collisions & Dictionary Pollution

### Summary

- 修正韓文 Mapping Engine 詞性碰撞與字典污染問題。
- 排除 `dict_ko_zh_tw.json` 被誤視為 Mapping 並載入 POS 的 Bug。
- 清理 `mapping_n.json` 中的副詞、代名詞、形容詞污染項 (25+ 項)。
- 在 `00_dictionary.json` 加入精確的詞性保護規則 (`adj`, `v`, `pron`, `num`, `det`)，成功解決 `사진` (Sajin) 誤拆問題。
- 總碰撞數從 49 降至 **42**，大幅提升自動鎖定率。

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | 23bf614 | done |
| release-aggregator | main | [TBD] | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 手動鎖定 `surface_resolution.jsonl` 中剩下的自然歧義項（如 `나`, `하고`, `가지`）。

---

## Session: 02:40 - Frontend Viewer Integration Phase 3 Implementation (Handoff: Continued in next session)

### Summary

- Completed FVI-10 & FVI-11: Implemented `AssetResolver` to determine local bundle vs cache paths.
- Implemented `AssetDownloadService` to manage on-demand Firebase Storage file downloads.
- Refactored `PrerecordedAudioService` and `GrammarNoteService` to utilize `AssetResolver`.
- Updated `EventOccurrenceScreen` with a download checking mechanism and UI overlay to fetch required remote assets before lesson start.
- Verified build and generated code successfully.

### Changed Repos

| Repo | Branch | Commit | Status |
| :--- | :--- | :--- | :--- |
| lingo-frontend-web | main | 5a985cf | done |
| release-aggregator | main | TBD | in-progress |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. Review and test the FVI Phase 3 functionalities manually (on iOS/macOS).
2. Execute `/wrap` closeout procedure for `FRONTEND_VIEWER_INTEGRATION` task.

---

## Session: 02:20 - POS Taxonomy Overhaul & Dictionary Cleanup

### Summary

- 建立韓文字典 POS 分類系統：新增 `PROP`（專有名詞）、`PHRASE`（慣用語）、`INTJ`（感嘆詞）三個新類別。
- 將人名（지수、민수、김、로라 等 10+ 項）從 `N/` 遷移至 `PROP/`。
- 將 `안녕하세요` 從 `TAG/` 遷移至 `PHRASE/`（問候語）。
- 將感嘆詞（네、아니요、아、오、음、와、에이）從 `TAG/` 遷移至 `INTJ/`。
- 刪除 18 個「殭屍 TAG」（名詞+助詞混合體，在 `N/` 或 `ADV/` 已有正確原子）。
- 更新 `engine/pos_taxonomy.py`：新增 PHRASE、INTJ POS 類型及 aliases。
- 更新 `engine/tokenizer.py`：加入 PRON、COP、PROP、PHRASE、INTJ 目錄到掃描清單。
- 修正 `ko__n__줄.json` headword 資料錯誤（民秀→줄）。
- 將 `scripts/tools/dict_viewer/data/audio/` 加入 `.gitignore` 並從 Git index 移除已追蹤音檔。
- 更新 `core-schema/schemas/atom.schema.json`：加入 COP、PROP、PHRASE 到 UPOS enum。
- 更新 `core-schema/schemas/dictionary_core.schema.json`：`pos` 欄位從自由字串改為有 enum 約束（19 個合法值）。
- 撰寫完整 POS 對照表至 `docs/SOPs/KO_DATA_STANDARDIZATION_PROTOCOL.md`。

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | 62658ee | done |
| core-schema | main | 8b8b4b8 | done |
| release-aggregator | main | TBD | done |

### Pending Decisions

- TAG 目錄剩餘 ~80 個殭屍條目（名詞+助詞未拆、未分解謂語）仍在，尚未清除。需先修正金標準的 `ko:tag:X` ID，才能安全刪除字典 TAG 條目。

### Blockers

- none（TAG 清理需要後續金標準 ID 修正，屬計劃性工作，非 blocker）

### Next Actions

1. 繼續清理金標準裡的 `ko:tag:` ID（寫腳本批量修正 A1 `.jsonl` 的 atom ID）。
2. 金標準修正後，刪除字典 `TAG/` 目錄剩餘的殭屍條目。
3. 重新執行 `prepare_viewer_data.py` 更新 Dict Viewer 數據。

---

## Session: 10:30 - A1-01 to A1-10 Gold Standard Overhaul (V5 Protocol)

### Summary

- 完成 A1-01 至 A1-10 金標準 (.jsonl) 全量大修，達成 V5 協議 100% 厚實原子化。
- **POS 重新分類**：將所有專有名詞（민수, 지수, 브래드, 피트）修正為 `ko:prop`；將感嘆詞與應答詞（네, 아니요, 아, 오, 와, 음, 자）修正為 `ko:intj`。
- **殭屍標籤清理 (Tag Decomposition)**：徹底分解並清除了所有殘留在金標準中的 `ko:tag:X` 條目（如 `비싸서요` -> `adj+e+e`、`씨는요` -> `n+p+p`、`아니면` -> `v+e` 等）。
- **同步文獻與提示詞**：
  - 更新 `KO_DATA_STANDARDIZATION_PROTOCOL.md` (SOP) 與 `KO_REVIEW_EXPERT_GUIDANCE.md` (專家指南)，納入新發現的分解模式。
  - 更新 Review Bot 核心提示詞 `PROMPT_SEGMENT_ONLY.md`，將 `ko:tag` 範例替換為正確的 V5 原子化標記，並同步最新 POS 分類。
  - 在控制塔 (`release-aggregator/docs/index.md`) 新增「韓文專屬規範」索引區塊。

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | b34eb83 | done |
| release-aggregator | main | 10b026d | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 延續本 Session 勢頭，執行 A1-11 至 A1-25 的金標準大修。
2. 待全量 A1 金標準更新後，物理刪除字典 `TAG/` 目錄下的所有殭屍原子條目。

---

## Session: 22:45 - A1-17 to A1-25 Gold Standard Cleanup (V5 Protocol)

### Summary

- 完成 A1-17 至 A1-25 金標準 (.jsonl) 全量大修，達成 V5 協議 100% 厚實原子化。
- **POS 重新分類**：將所有專有名詞（지수, 민수, 톰, 케이티, 제이슨, 베티, 로라, 밥, 조, 소라）修正為 `ko:prop`；將感嘆詞與應答詞（네, 아니요, 아, 오, 와, 음, 에이）從 `ko:tag` 修正為 `ko:intj`。
- **殭屍標籤清理 (Tag Decomposition)**：徹底分解並清除了所有殘留在金標準中的 `ko:tag:X` 條目（如 `정말요` -> `n+p`、`없던데요` -> `adj+e+e`、`일하고` -> `v+e` 等）。
- **修正對齊錯誤**：修正了 A1-23 中發現的嚴重 ID 對齊位移錯誤，確保金標準數據準確性。

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | [TBD] | done |
| release-aggregator | main | [TBD] | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 待全量 A1 金標準更新後，執行字典清理，移除 `TAG/` 目錄下的殭屍原子。
2. 進行 A1 字典與金標準的最終一致性驗證。
3. 啟動 A1-11 之後的金標準審核流程。

---

## Session: 04:00 - A1-01 to A1-10 GSD Final Review & A1-11 Preparation

### Summary

- **A1-01 to A1-10 GSD 最終審核**：使用 `gsd_surgeon.py` 進行二次校對，確保 100% 符合 V5 協議。
- **精確 POS 修正**：
  - 修正 A1-04 `오` (ref: A1-04:L04-D2-03:0) 從 `ko:intj:음` 變更為 `ko:intj:오`。
  - 驗證 `있다`, `없다`, `아니다` 均已標記為 `ko:adj`（如 A1-07 `아니면` -> `ko:adj:아니다+ko:e:면`）。
- **A1-11 準備**：完成 A1-11 金標準 dump 為 `A1-11_review.json`，供下一階段審核使用。
- **工具優化**：驗證 `gsd_surgeon.py apply` 機制能正確迴傳並標記 `MANUAL_SURGERY`。

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | [TBD] | done |

### Pending Decisions

- none

### Blockers

- none

---

## Session: 12:25 - A1-17 to A1-25 Gold Standard Precision Review (Complete)

### Summary

- **A1-17 to A1-25 GSD 深度審理**：完成 A1 系列最後 9 門課的金標準複核與寫回，達成 A1 全系列 100% V5 協議鎖定。
- **原子化修正**：
  - 分解複合詞尾：`ㄴ데요` -> `ㄴ데+요` (A1-18, A1-20, A1-22)。
  - 標準化尊稱代詞：將 `이분`, `저분` 從 `ko:pron` 拆解為 `ko:det + ko:n` (A1-25)。
  - 修正詞性：`달러` -> `ko:count`；`잘하다`/`못하다` -> `ko:v` (單一 Lemma)。
- **慣用語映射更新**：在 `mapping_phrase.json` 新增 `물론이죠`, `그렇군요`, `그렇죠` 等 3 項公式化表達，確保 UI 解析完整。
- **數據閉環**：使用 `gsd_surgeon.py` 將所有修正寫回金標準，並標記 `MANUAL_SURGERY`。

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | d422781 | done |
| release-aggregator | main | e186a0a | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 啟動 A2 系列 (KO-GEMINI-03-A2) 的金標準審核。
2. 執行 `build_dictionary.py` 同步最新詞性與慣用語映射。

---

## Session: 12:50 - Frontend A1 Assets Upgrade & Dictionary ID Normalization

### Summary

- **Upgrade A1 Dialogue Assets**: Updated `assets/content/production/packages/ko/yarn/` for A1-01 through A1-25 with latest atomic segmentation data (`atoms`) and `zh_TW` locale keys.
- **Dictionary ID Normalization**: Implemented `DictionaryParser.normalizeId` and integrated it into `DictionaryContent` to bridge mismatch between colon-separated content IDs (e.g., `ko:n:apple`) and underscore-separated dictionary keys (e.g., `ko_N_apple`).
- **Locale Standardization**: Handled `zh-TW` to `zh_TW` mapping in dictionary lookups for consistent fallback to embedded translations.
- **UI Styling Compatibility**: Reverted `withValues` to `withAlpha` in `DictionaryContent` for better compatibility with target Flutter environment.

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | 12be602 | done |
| release-aggregator | main | [pending] | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 啟動 A2 階層內容的前端同步與顯示驗證。
2. 進行 Flutter App 在實機上的下載與離線緩存機制測試.

---

## Session: 13:50 - Resolve Dictionary Missing Definition on Restart

### Summary

- Fixed a silent race condition bug causing dictionary lookups to fail with "no definition" upon app restart.
- Modified `DictionaryService` and its related providers to be `@Riverpod(keepAlive: true)`, preventing the internal language pack state (`_v4LanguagePacks`) from being erroneously cleared due to provider auto-disposal.
- Synchronously awaited `appConfigProvider` during dictionary initialization in `EventOccurrenceScreen` to guarantee correct loading locales before startup.
- Added structured `Logger` calls and cleaned up obsolete `print` debug lines which were failing CI lint checks.

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | fb6ae46 | done |
| release-aggregator | main | [pending] | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. Re-verify the stability of Dictionary initialization and asset routing on a clean simulator environment.

---

## Session: 14:35 - Dictionary Hotfix Verification & Cleanup

### Summary

- Verified Dictionary Hotfix: Confirmed `ko:tag:안녕하세요` normalizes to `ko_PHRASE_안녕하세요` and lookups succeed in `zh_tw`.
- Confirmed `@Riverpod(keepAlive: true)` persistence prevents data loss on restart.
- Cleaned up debug logs and unused `Logger` imports in `DictionaryResolver` and `DictionaryParser`.
- Verified that `EventOccurrenceScreen` correctly awaits config before dictionary initialization.

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | f5a113e | done |
| release-aggregator | main | [pending] | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. Wait for regular content cleansing/updates.
2. Re-verify with fresh data after next ingestion.

---

## Session: 15:00 - Rule Engine Optimization and POS Tag Standardization

### Summary

- 執行了 POS 標籤標準化，將全域的 `ko:tag` 替換為 `ko:intj`，`ko:v:이다` 修正為 `ko:cop:이다`，並涵蓋已生成的 A1-01 至 A1-10 金標準檔案與 override 設定檔。
- 完成深度原子化拆解 (Deep Atomic Surgery)，針對 `30_verb_endings.json` 複合詞尾進行遞迴拆解（如 `세요` -> `시` + `어요`，`셨어요` -> `시` + `었` + `어요`）。
- 實作引擎結構重構 (Structural Engine Refactoring)，移除 `rules_engine.py` 內硬編碼的 ID 檢查，改為基於 `feature` 的數據驅動屬性（`ss_contraction`, `batchim_suffix`）。
- 執行 `stage2_executor.py` 驗證，17,800+ 筆資料達成 1.0 Reconstruction Pass Rate，無碰撞退化。

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | 0556054 | done |
| release-aggregator | main | TBD | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 觀察後續新課程 (A1-11+) 的驗證結果，確保新的 POS 標籤與原子拆解完美適應。

---

## Session: 16:20 - V5 Standardization for A1-11 to A1-25

### Summary

- 完成 A1-11 至 A1-25 金標準與 Override 檔案之 V5 標準化（厚實原子化）。
- **Tag 遷移與深層大修**：同步更新 `engine/tokenizer.py` 與 `run_mapping_pipeline.py`；執行 `v5_surgical_decomposition.py` 針對 zombie tags（如 `씨는요`, `비싸서요`, `20분이요`, 人名等）進行深層原子拆解或 POS 修正 (`ko:prop`)。
- **謂語分解**：針對 `세요`, `셨어요`, `는데요`, `지요` 等複合詞尾執行深度遞迴拆解。
- **自動化 Override**：撰寫腳本從金標準生成 lesson-specific overrides，成功鎖定 Mapping 決策，達成 1.0 Reconstruction Pass Rate 並通過 Hard Gates（目標範圍內 0 衝突）。
- **字典清理**：刪除 `INTJ` 目錄下的所有非感嘆詞殭屍條目，僅保留 8 個標準感嘆詞。

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | 0d25263 | done |
| release-aggregator | main | 1e57608 | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 延續大修節奏，啟動 A2 系列之 V5 標準化與原子化拆解。

---

## Session: 17:15 - A1 Overrides Rationality Audit & Banmal Support Planning

### Summary

- Completed a detailed linguistic rationality audit of A1 Overrides (A1-11 to A1-25) against V5 standardization protocol.
- Audited `engine/rules_engine.py` architecture and rule sets. Found it robust but lacking context-awareness for the `요` particle and Banmal support.
- Identified redundancies in overrides (e.g., `있어요` 50x) that should be removed by relying on the engine.
- Investigated Banmal (Informal Speech) support and created a comprehensive implementation plan to expand the Rules Engine for A2/B1.
- Constructed a step-by-step task checklist and a seamless handoff prompt for the upcoming optimization phase.

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | f9e9c49 | done |
| release-aggregator | main | [pending] | in-progress |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. In the next session: Implement `요` contextual interceptor, optimize rule priorities, clean up A1 overrides, and add Banmal support.

---

## Session: 17:15 - A1 V5 Full Audit & Dictionary Pipeline Optimization

### Summary

- Completed **A1 Gold Standard Full Audit** for V5 protocol compliance (A1-01 to A1-25).
- Surgically decomposed polymerized adjectival/verbal endings (e.g., `세요` -> `시`+`어요`, `했어요` -> `하다`+`았`+`어요`).
- Synchronized Dictionary via `build_dictionary.py` to auto-generate missing atoms (e.g., `ko:intj:네`, `ko:prop:민수`) into `content/core/dictionary/atoms/`.
- Validated via `ko_data_pipeline.py` and successfully passed with 0 Errors (perfect standard and dictionary mappings).
- Pipeline is complete and well-tested for A1 data structure. All dictionary atoms generated correctly sync to i18n JSONs via build scripts.

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | f9e9c49 | done |
| release-aggregator | main | pending | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. Start A2 baseline review.

---

## Session: 17:30 - A2 Review Pipeline Optimization Planning

### Summary

- 分析現有 Review 流程痛點：測試痕跡四散於 `.codex_review/` 與 `data/review_history/`，且腳本執行過於破碎。
- 提出 **"Clean Workspace"** 架構：
  - 統一工作區至 `data/reviews/`。
  - 收攏腳本至 `scripts/review/` 並標準化命名 (01-04)。
  - 引入 `orchestrator.py` 封裝「自動生成 Draft」與「人工 Apply Patch」兩大階段。
- 撰寫 `docs/handoffs/A2_REVIEW_PIPELINE_OPTIMIZATION_PLAN.md` 作為下個 Session 的執行藍圖。

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | 9818897 | done |
| release-aggregator | main | [pending] | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 執行 A2 Review Pipeline 優化計畫：大掃除舊有實驗資料、遷移並重構腳本、撰寫 Orchestrator 並更新 SOP 文件。

---

## Session: 18:00 - A2 Review Pipeline Optimization Completed

### Summary

- **Archived Obsolete Artifacts**: Verified `data/review_history` and `.codex_review` were archived, deleted leftover tools like `gsd_surgeon.py` from `scripts/tools/` and removed legacy runbook files like `KO_GEMINI_REVIEW_BOT_CHUNK_AND_GOLDEN_RUNBOOK_2026-02-18.md` and `KO_GEMINI_MAPPING_REVIEW_RUNBOOK_2026-02-16.md`.
- **Drafted Orchestrator**: Finalized `scripts/review/orchestrator.py` enabling streamlined `draft` and `apply` phases for the Golden Standard review process. Fixed path constants in `03_build_gold.py`.
- **Updated SOPs**: Synthesized the instructions into a unified A2 workflow document at `docs/SOPs/KO_A2_REVIEW_WORKFLOW.md`. Deleted the A2 optimization handoff plan as the tasks are completed.

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | [pending] | done |
| release-aggregator | main | [pending] | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. Start testing and running the new `orchestrator.py` on the A2 baseline data.

## Session: 17:40 - A1 Dictionary Translation Audit & Patching

### Summary

- 完成 A1 韓文詞典 zh-TW 翻譯全量稽核，識別並補正 41 項缺漏詞條（包含 INTJ, PROP, E, VX 等新產出原子）。
- 修正系統性 POS 定義錯誤：將誤植為語尾 (E) 定義的四大關鍵助詞 `은`, `는`, `을`, `를` (及其收縮形 `ㄴ`, `ㄹ`) 修正回正確的「助詞 (P)」定義。
- 通過 `ko_data_pipeline.py` 驗證，達成 0 Errors (1:1 correspondence for A1-01~A1-25)。
- 字典與 Mapping JSON 已同步至最新狀態。

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | e25987e | done |
| release-aggregator | main | [pending] | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 繼續執行 A2 系列之 V5 標準化與金標準審核。
2. 進行 A2 階段之翻譯補全。

---

## Session: 18:11 - Rules Engine Optimization & Banmal Support

### Summary

- Extended the Core Rules Engine with Banmal (Informal Speech) support by adding rules `31_verb_endings_banmal.json` and `41_copula_banmal.json` for verb endings and copulas.
- Fixed a directory crawling bug in rule dictionary loader to include subdirectories.
- Prevented over-lemmatization of `exact` dictionary rules (e.g., `먹어`).
- Implemented `Try-Engine-First` logic for `generate_overrides_from_gold.py`, significantly reducing redundant overrides (removed 124 existing redundancies from A1 jsonl datasets).
- Validated all 25 A1 lessons with Python unit testing and full mapping pipeline (0 Errors).

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | f63db87 | done |
| release-aggregator | main | [pending] | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. Start A2 baseline review or integrate local Qwen3TTS for audio generation.

---

## Session: 19:05 - V5-A1 Content Migration & Pipeline Standardization

### Summary

- **穩定版去冗餘與測試修復**：物理刪除 `lingo-frontend-web` 穩定版路徑下的非韓語資產（de, th, id 等），並同步修正了 `pubspec.yaml`、`video_metadata.json` 以及數個連鎖報錯的測試案例（Asset Integrity, Event Integration, Golden Tests），確保穩定版 `production` 環境純淨且全量測試全綠 (269+ tests passed)。
- **發佈策略定案**：在 `RELEASE_ASSET_STRATEGY.md` 正式定義「穩定版（純淨韓語）vs. 測試版（全語系）」的分離策略。
- **任務排期**：在 Aggregator 建立 `CONTENT_V5_MIGRATION_L0` (L0 遷移計畫) 與 `STABLE_TEST_RCA` (測試報錯根因分析) 兩項新任務。
- **Pipeline 升級**：
  - 更新 `content-pipeline` 支援 V5 分層目錄（core/i18n）彙整，並整合 **語音包 (Audio Pack)** 處理邏輯。
  - 升級 `release-aggregator` 的 `release.py` 以支援非 JSON 資產（ogg, mp3）及 V5 冒號格式 ID。
- **前端相容性修復**：修正 `DictionaryParser` 以雙向支援 Legacy（底線）與 V5（冒號）ID 格式，並實作辭典增量合併機制。
- **準發佈包產出**：成功產出 `v1.1.0-pre` 準發佈包，包含 3242 個資產項（文字 + 詞典 + 文法 + 語音）。
- **任務排期**：在 Aggregator 建立 `CONTENT_V5_MIGRATION_L0` 任務，追蹤舊版 L0 內容往 V5 格式的遷移計畫。

### Changed Repos

| Repo | Branch | Commit | Status |
| :--- | :--- | :--- | :--- |
| content-ko | main | c24e610 | done |
| content-pipeline | main | a668fcb | done |
| lingo-frontend-web | main | 0d5368b | done |
| release-aggregator | main | 8b35581 | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 依照 `CONTENT_V5_MIGRATION_L0.md` 啟動舊版內容的 V5 分解與轉換。
2. 驗證 V5 辭典在前端的性能表現（條目數增加至 2200+）。

---

## Session: 19:30 - Core Schema Split and Pipeline Update for i18n Separation

### Summary

- **Schema 拆分**：為符合 i18n 標準，將 `core-schema` 內的 `dialogue.schema.json` 拆分為 `dialogue_core.schema.json`（僅含原文與角色）與 `dialogue_i18n.schema.json`（僅含翻譯），舊有規則保留為 `dialogue_legacy.schema.json`。
- **Pipeline 更新**：修改 `content-pipeline` 內的 `build_ko_zh_tw.py`，取消原本統一合併原文與翻譯的邏輯，改為將核心原文與翻譯內容分別輸出到對應的核心路徑 (`ko/core/dialogue`) 與翻譯路徑 (`ko/zh_tw/dialogue`)。
- **建置過程強制校驗**：在 `content-pipeline` 的打包過程中整合 `core-schema` 的校驗腳本，確保所有產出檔案完全符合新制訂的 Schema。
- **更新發布打包與前端資產**：更新 `release-aggregator` 以產出 `v1.1.0-pre` 準發布包，實現各語系翻譯分離，並同步更新前端與 Lllo Viewer 的參照路徑（移除已過時的 `9_production`）。

### Changed Repos

| Repo               | Branch | Commit  | Status |
|--------------------|--------|---------|--------|
| lingo-frontend-web | main   | c379e92 | done   |
| content-ko         | main   | 3eed0f6 | done   |
| content-pipeline   | main   | 9a62ee5 | done   |
| core-schema        | main   | 645605d | done   |
| lllo               | main   | 7d5001b | done   |
| release-aggregator | main   | 5f566a3 | done   |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 確認 lingo-frontend-web 在完全分離 i18n 資產包後的運行與載入情形是否正常。
2. 檢查各其他模組是否還有零星的 `9_production` 硬編碼需要更新。

---

## Session: 21:50 - Frontend App Dictionary & Audio Fallback Fixes

### Summary

- 修復了 `안녕하세요` 等常見片語在單詞釋義區的首列會「強制渲染空白」的問題，採用依據空邏輯判斷是否隱藏上方釋義。
- 移除了原先用來防錯的 hardcoded fallback，改在 `MAPPING_DICTIONARY_TASKS.json` 建立 `KO-DICT-04`，未來透過 pipeline 注入真實釋義。
- 修復了 `AssetResolver` 在 macOS 環境下讀取編碼引發的播放問題（NFC/NFD mismatch timeout）。
- 確認修復後 Dictionary 與音檔功能（PrerecordedAudioService）可在 Flutter UI 正常運作，且無多餘 UI。

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| lingo-frontend-web | main | 27b8307 | done |
| release-aggregator | main | bc58847 | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. Pipeline 團隊執行 `KO-DICT-04`，確保 Phrase 的意思能被自動注入 i18n JSON 字典檔。
2. 啟動韓文 B2/C1 課程優化任務。

---

## Session: 23:10 - KO B2/C1 Optimization Plan & Infrastructure

### Summary

- 完成韓文 B2/C1 課程優化 Phase 1：更新 `persona_governance.md` 定義 C1 評論家角色，並建立專屬計畫書與任務追蹤 JSON。
- 角色定案：B2 為「正經商務 (Manager Kim & Director Lee)」，C1 為「深度評論 (Professor Park & Critic Choi)」。
- 已在 `TASK_INDEX.md` 同步更新任務狀態。

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| lllo | main | c605a66 | done |
| release-aggregator | main | 6210dcd | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 執行 KO-B2-01 (B2 語氣與角色校對)：將 L01-L25 改寫為商務正式體 (`-(스)ㅂ니다`)。

## Session: 23:14 - A2 Ingestion & V5 Surgeon Prep

### Summary

- **A2 Ingestion**: Completed full A2 curriculum (L01-25) import from `lllo` repository to `content-ko`.
- **Pipeline Infrastructure**:
  - Fixed `run_rule_tests.py` import path and `run_mapping_pipeline.py` report naming logic.
  - Formalized **KO REVIEW WORKFLOW** in `docs/SOPs/KO_A2_REVIEW_WORKFLOW.md` to prioritize Agent-driven manual review (V5 Surgeon) for speed and stability.
- **V5 Surgeon Prep**: Executed initial A2 mapping pipeline and generated `surgery_A2-01.json` for lesson-level precision audit.
- **Task Tracking**: Updated `MAPPING_DICTIONARY_TASKS.json` and `KO_GEMINI_REVIEW_TASKS.json` status to `IN_PROGRESS` for A2-related tasks.

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| content-ko | main | 821e126 | done |
| release-aggregator | main | 0f76fbe | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. **A2-01 Manual Surgery**: Execute full Agent review on `surgery_A2-01.json` to lock the A2-01 golden baseline.
2. **Sequential Rollout**: Proceed to A2-02~25 once the pilot A2-01 process is finalized.

---

## Session: 23:35 - Dialogue UI Redesign (Zen Study) Planning

### Summary

- 定義了 "Zen Study" UI 改版策略：從底部的對話框轉向居中對焦卡片 (Focus Card) 與迷你頭像。
- 規劃了「雙層垂直字典 (Double-layered Vertical Dictionary)」：針對 Phrase (慣用語/片語) 提供整體釋義與底層原子拆解 (Atoms) 的垂直列表。
- 制定了 4 階段執行計畫 (DUI-01~07)：採用平行開發模式建立新組件，避免破壞現有對話介面。
- 翻譯控制與語法筆記按鈕將整合至螢幕右上角。

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| release-aggregator | main | 9192a85 | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 執行 DUI-01：建立 `ZenEventDialogueBox` 與 `ZenAvatar` 組件並實作置中佈局。
2. 執行 DUI-02/03：完成翻譯切換動畫與右上角 TopBar 改版。

---

## Session: 23:35 - Dialogue UI Redesign (Zen Study) Planning

### Summary

- 定義了 "Zen Study" UI 改版策略：從底部的對話框轉向居中對焦卡片 (Focus Card) 與迷你頭像。
- 規劃了「雙層垂直字典 (Double-layered Vertical Dictionary)」：針對 Phrase (慣用語/片語) 提供整體釋義與底層原子拆解 (Atoms) 的垂直列表。
- 制定了 4 階段執行計畫 (DUI-01~07)：採用平行開發模式建立新組件，避免破壞現有對話介面。
- 翻譯控制與語法筆記按鈕將整合至螢幕右上角。

### Changed Repos

| Repo | Branch | Commit | Status |
|---|---|---|---|
| release-aggregator | main | 9192a85 | done |

### Pending Decisions

- none

### Blockers

- none

### Next Actions

1. 執行 DUI-01：建立 `ZenEventDialogueBox` 與 `ZenAvatar` 組件並實作置中佈局。
2. 執行 DUI-02/03：完成翻譯切換動畫與右上角 TopBar 改版。
